# ✅ React Router SPA fallback (ensures all routes resolve to index.html)
# Prevents 404s on page refresh
/*    /index.html   200

# ✅ Permanent redirects for old “/pages/” paths (SEO safe)
# These ensure old indexed URLs pass link juice to the correct page
/pages/services/*     /services/:splat    301
/pages/privacy-policy /privacy-policy     301
/pages/terms-and-conditions /terms-and-conditions 301

# ✅ Optional: Redirect old or renamed service URLs (customizable)
# /old-service-path    /services/app-development   301

# ✅ Force HTTPS (security + ranking factor)
http://*  https://:splat  301!
